source 'https://github.com/Modool/cocoapods-specs.git'

inhibit_all_warnings!

platform :ios, '8.0'
use_frameworks!

abstract_target 'MDSegmentController_targets' do
    pod 'MDHorizontalListView', '~> 1.0.0'

    target 'MDSegmentController' do
    end
end
